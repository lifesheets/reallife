var Animations = require("../libs/animations.js");
var Interaction = require("../interaction");
var Vehicles = require("../models/vehicles.js");


mp.events.addCommand("dvehicle", (player, fullText, ...arg) => {
	let pos = player.position;
	let model = args[0];


	Vehicles.create({




	})

});